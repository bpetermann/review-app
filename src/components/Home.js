import React from 'react';
// import classes from './Home.module.css';
import Searchbar from './Searchbar';

const Home = (props) => {
  return (
    <React.Fragment>
      <Searchbar />
    </React.Fragment>
  );
};

export default Home;
