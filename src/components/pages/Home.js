import React from 'react';
import Searchbar from '../Searchbar/Searchbar';

const Home = (props) => {
  return (
    <React.Fragment>
      <Searchbar />
    </React.Fragment>
  );
};

export default Home;
